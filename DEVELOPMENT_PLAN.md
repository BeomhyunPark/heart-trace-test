# 2026 여름수련회 마음의 흔적 테스트 웹앱 — 개발 설계안

## 1. 현재 작업 폴더 상태

- 작업 위치: `/Users/hyunee/.codex/.chatgpt-projects/g-p-6a674c92379c8191b209638579a675cf`
- 앱 소스, 패키지 설정, 빌드 설정, Git 저장소가 아직 없다.
- 현재 파일은 프로젝트 안내용 `AGENTS.md`와 읽기 전용 참고자료인 `sources/`뿐이다.
- 따라서 기존 기술이나 폴더 구조를 유지할 필요가 없는 신규 프로젝트로 본다.
- 이번 단계에서는 앱 초기화나 소스 구현을 하지 않는다.

## 2. 확정 기술 스택

### 권장 확정안

| 영역 | 선택 | 이유 |
|---|---|---|
| 앱 | React + TypeScript | 3개 주요 화면과 답변 상태를 작고 명확하게 관리하기 좋다. |
| 빌드 | Vite | 정적 모바일 웹앱에 충분하고 빌드 결과가 가볍다. |
| 스타일 | CSS Modules 또는 단일 전역 CSS | 디자인 규모가 작아 별도 UI 프레임워크가 필요 없다. |
| 화면 전환 | React 내부 상태 머신 | URL 라우터가 필요 없는 단일 흐름이며 새로고침 초기화 요구와 잘 맞는다. |
| 상태 관리 | `useReducer` + Context(필요 시) | Redux/Zustand 없이도 답변·현재 문항·화면 상태를 예측 가능하게 관리한다. |
| 테스트 | Vitest + React Testing Library | 점수, 동점, 이전 답변 변경, 화면 흐름을 검증한다. |
| 배포 | 정적 호스팅/CDN | 서버와 DB가 없고 150~200명 접속을 충분히 처리할 수 있다. |

### 의도적으로 제외하는 것

- 서버 API, 데이터베이스, 로그인, 개인정보 입력, 분석 도구
- `localStorage`, `sessionStorage`, 쿠키를 이용한 답변 저장
- React Router: 공유 가능한 개별 URL이 없고, 새로고침 시 무조건 처음으로 돌아가야 하므로 이 앱에서는 불필요하다.
- 대형 상태 관리·UI 라이브러리
- 첫 버전의 PWA/오프라인 설치 기능

## 3. 권장 폴더 구조

```text
heart-trace-test/
├─ public/
│  ├─ images/
│  │  ├─ characters/       # 캐릭터 및 화면용 이미지
│  │  └─ result-cards/     # 저장/공유용 결과 이미지 5종
│  └─ fonts/               # 실제 사용 권리가 확인된 폰트만 포함
├─ src/
│  ├─ app/
│  │  ├─ App.tsx           # 최상위 화면 분기
│  │  └─ testReducer.ts    # 검사 상태와 이벤트
│  ├─ components/
│  │  ├─ AnswerOption.tsx
│  │  ├─ ProgressBar.tsx
│  │  └─ ScreenLayout.tsx
│  ├─ screens/
│  │  ├─ IntroScreen.tsx
│  │  ├─ QuestionScreen.tsx
│  │  └─ ResultScreen.tsx
│  ├─ data/
│  │  ├─ questions.ts      # Google Docs 확정 20문항과 A~E 매핑
│  │  └─ resultTypes.ts    # Figma 확정 5유형 문구·이미지·색상
│  ├─ domain/
│  │  ├─ types.ts          # Question, Answer, ResultType 등
│  │  ├─ scoring.ts        # 점수 집계
│  │  └─ tieBreaker.ts     # 동점 처리 정책
│  ├─ utils/
│  │  └─ resultImage.ts    # 이미지 공유·저장·폴백 처리
│  ├─ styles/
│  │  ├─ globals.css
│  │  └─ tokens.css        # Figma 색상·간격·타이포 토큰
│  └─ main.tsx
├─ tests/
│  ├─ questions.integrity.test.ts
│  ├─ scoring.test.ts
│  └─ test-flow.test.tsx
├─ index.html
├─ package.json
├─ tsconfig.json
└─ vite.config.ts
```

화면이 작기 때문에 처음부터 폴더를 더 세분화하지 않는다. 한 파일이 과도하게 커질 때만 분리한다.

## 4. 핵심 설계

### 4.1 화면 흐름과 라우팅

URL 경로는 하나만 사용한다. 앱 내부의 `phase`에 따라 화면을 바꾼다.

```text
intro → question(1~20) → result
          ↑ 이전/답변 변경
```

- 시작 버튼: `intro`에서 1번 문항으로 이동
- 답변 선택: 해당 문항 답변을 덮어쓴 뒤 즉시 다음 문항으로 이동
- 20번 답변 선택: 점수를 계산하고 결과 화면으로 이동
- 이전 버튼: 직전 문항으로 이동하며 기존 선택을 표시
- 다시 하기: 상태 전체를 초기값으로 되돌림
- 새로고침/탭 재접속: 브라우저 저장소를 쓰지 않으므로 항상 처음 화면
- 브라우저 뒤로가기는 앱 내부 문항 이동 수단으로 사용하지 않는다. 화면 안의 이전 버튼을 제공한다.

필요한 상태의 최소 형태:

```ts
type Phase = 'intro' | 'question' | 'result';

type TestState = {
  phase: Phase;
  currentQuestionIndex: number;
  answers: Array<ResultTypeId | null>; // 길이 20
  result: ResultTypeId | null;
};
```

이벤트는 `START`, `ANSWER`, `PREVIOUS`, `RESTART` 네 가지를 기본으로 한다. 화면 전환 규칙은 컴포넌트 곳곳에 흩어 두지 않고 reducer 한 곳에서 처리한다.

### 4.2 질문 데이터

질문은 UI 코드와 분리된 정적 TypeScript 데이터로 관리한다.

```ts
type Question = {
  id: number;
  text: string;
  options: Array<{
    id: 'A' | 'B' | 'C' | 'D' | 'E';
    text: string;
    resultType: ResultTypeId;
  }>;
};
```

데이터 입력 기준:

- Google Docs의 `t.gp9cfhyqeusg` 탭만 사용
- 문항 20개, 문항마다 A~E 5개를 그대로 전사
- 하단 매핑표를 각 선택지의 `resultType`으로 입력
- 화면 줄바꿈을 위해 질문 원문에 임의의 개행을 넣지 않고 CSS로 처리
- 무결성 테스트로 문항 수 20개, 각 선택지 수 5개, A~E 중복 없음, 유효한 유형 매핑을 자동 확인

### 4.3 유형 데이터

유형 식별자는 화면 문구와 분리해 안정적인 영문 키를 사용한다.

| ID 예시 | 유형 | 흔적 | 새겨질 흔적 |
|---|---|---|---|
| `bear` | 곰곰이 | 붙잡음의 흔적 | 위로 · 용서 · 자유 |
| `spring` | 봄봄이 | 감춤의 흔적 | 사랑 · 담대함 · 정체성 |
| `effort` | 낑낑이 | 애씀의 흔적 | 은혜 · 균형 · 신뢰 |
| `pause` | 숨숨이 | 숨 고르기의 흔적 | 용기 · 공동체 · 소망 |
| `express` | 톡톡이 | 표출의 흔적 | 온유 · 인내 · 평안 |

- 결과 문구와 표현의 최종 기준은 Figma `05 Screens / 최종`이다.
- `낑낑이 = 애씀의 흔적`, `톡톡이에게 새겨질 흔적` 수정사항을 회귀 테스트 체크리스트에 포함한다.
- 실제 영문 ID는 구현 시작 전에 한 번 정한 뒤 문구 수정과 무관하게 유지한다.

### 4.4 점수 계산과 답변 변경

점수는 별도로 누적 저장하지 않고 `answers`에서 파생 계산한다.

1. 모든 유형 점수를 0으로 만든다.
2. 답변 20개를 순회한다.
3. 각 답변이 가리키는 유형에 1점을 더한다.
4. 최고 점수를 가진 유형 목록을 만든다.
5. 한 유형이면 확정하고, 여러 유형이면 동점 정책 함수에 넘긴다.

이 방식은 이전 문항에서 답을 바꿀 때 “기존 점수 차감 후 새 점수 가산”을 직접 관리하지 않아도 된다. 답변을 덮어쓰고 전체 20개를 다시 집계하면 되며 계산량은 무시할 수 있을 만큼 작다.

필수 테스트:

- 단독 최고점 5유형 각각
- 2개 이상 동점
- 이전 답변 변경 후 기존 유형 점수가 빠지고 새 유형에 반영됨
- 마지막 문항 변경 후 결과도 변경됨
- 답변 누락 상태에서는 결과를 생성하지 않음

### 4.5 동점 처리

동점 규칙은 아직 제품 결정이므로 `resolveTie(tiedTypes, answers)`로 분리한다. 다른 코드 변경 없이 정책만 교체할 수 있게 한다.

권장안은 **전체 답변 순서를 기반으로 만든 결정적 해시값으로 동점 후보 중 하나를 선택**하는 방식이다.

- 같은 20개 답변에는 항상 같은 결과가 나온다.
- 특정 캐릭터가 고정 우선순위를 가져 생기는 편향을 줄인다.
- 추가 질문, 서버, 저장소가 필요 없다.
- 모든 본 문항의 점수 가중치는 여전히 동일하다.

단, 참가자에게 설명하기 쉬운 규칙을 더 중요하게 본다면 “동점 후보를 함께 표시”하거나 “동점 전용 1문항 추가”도 가능하다. 후자는 총 20문항이라는 현재 기준을 바꾸므로 별도 승인 없이는 채택하지 않는다.

### 4.6 결과 이미지 저장과 공유

개인화되는 이름이나 점수가 없으므로 화면을 캡처하는 라이브러리보다 **Figma에서 결과 카드 5종을 이미지로 미리 내보내는 방식**을 권장한다.

- 결과 화면의 유형에 맞는 정적 결과 이미지를 선택
- 지원 기기에서는 Web Share API로 이미지 파일 공유
- 공유가 불가능한 환경에서는 파일 다운로드 제공
- iOS Safari에서 자동 저장이 제한되면 새 탭으로 이미지를 열고 “사진에 저장” 안내 제공
- 버튼은 `결과 이미지 저장`과 `공유하기`를 분리할 수 있으나 Figma 최종 UI를 우선
- 이미지 파일에는 개인 답변이나 점수를 넣지 않음
- 이미지 내 문구도 접근성 텍스트로 화면에 제공해 이미지에만 의미를 의존하지 않음

권장 자산 기준:

- 원본: Figma에서 고해상도 PNG 내보내기
- 웹 표시용은 필요하면 WebP로 최적화하되, 저장/공유용 PNG는 별도 유지
- 파일명은 `result-bear.png`처럼 영문·고정 규칙 사용
- 결과 이미지 5종은 검사 시작 후 백그라운드에서 미리 불러와 결과 화면 대기를 줄임

### 4.7 모바일 접근성 및 사용성

- 기준 화면 폭 320px 이상, 본문 최대 폭 약 480px
- `100dvh`와 `env(safe-area-inset-*)`를 사용해 모바일 주소창·노치 대응
- 선택지와 주요 버튼 터치 영역 최소 44×44px, 권장 높이 48px 이상
- 질문과 선택지는 의미상 `fieldset`/`legend`와 radio 구조 사용
- 기존 답변, 현재 문항, 전체 문항 수를 화면과 보조기기에 전달
- 진행률 변화는 필요 시 `aria-live="polite"`로 알리되 과도한 낭독 방지
- 키보드 포커스 표시 유지, 색상만으로 선택 상태를 구분하지 않음
- WCAG AA 수준의 텍스트 대비 확보
- 애니메이션은 짧게 하고 `prefers-reduced-motion` 존중
- 선택 직후 즉시 다음 문항으로 이동하되, 중복 탭으로 두 문항이 넘어가지 않도록 한 번의 이벤트만 처리
- 고정 하단 버튼이 기기 홈 표시줄이나 키보드에 가리지 않도록 안전 영역 반영
- 느린 이동통신에서도 첫 화면이 빠르게 뜨도록 이미지 크기와 웹폰트 사용을 제한

## 5. 단계별 구현 계획

### 0단계 — 미확정 사항 결정 및 원본 확보

- 동점 규칙 승인
- Figma 최종 화면의 색상, 폰트, 간격, 이미지 자산 내보내기
- Google Docs 확정 탭의 20문항과 매핑표를 2인 검수 가능한 형태로 확보
- 결과 이미지 저장/공유 버튼의 최종 문구와 동작 확정
- 배포 주소와 호스팅 선택

### 1단계 — 프로젝트 골격

- React + TypeScript + Vite 프로젝트 초기화
- 기본 전역 스타일, 디자인 토큰, 모바일 viewport 설정
- 검사 도메인 타입과 5개 결과 유형 정의

### 2단계 — 데이터와 계산 코어

- 질문 20개와 A~E 매핑 입력
- 질문 데이터 무결성 검사 작성
- 순수 함수 형태의 점수 계산과 교체 가능한 동점 처리 구현
- 점수 단위 테스트 작성

### 3단계 — 검사 상태 흐름

- reducer와 `START`/`ANSWER`/`PREVIOUS`/`RESTART` 이벤트 구현
- 답변 즉시 이동, 이전 답변 표시, 답변 변경, 마지막 문항 완료 처리
- 새로고침 시 초기화됨을 확인

### 4단계 — Figma 화면 구현

- 시작, 질문, 결과 화면을 최종 Figma 기준으로 구현
- 320px부터 일반적인 iPhone·Galaxy 폭까지 반응형 조정
- 진행률, 선택 상태, 로딩/전환 상태 구현

### 5단계 — 결과 저장·공유

- 결과 카드 5종 연결 및 사전 로딩
- Web Share API와 다운로드 폴백 구현
- iOS Safari 저장 안내 처리

### 6단계 — 접근성·안정성 점검

- 키보드, 스크린리더 구조, 색 대비, 터치 영역, reduced motion 점검
- 연속 탭, 이전 답변 반복 수정, 결과 후 다시 하기 등 경계 상황 점검
- 실제 iPhone Safari와 Android Chrome에서 확인

### 7단계 — 배포 전 검증

- 질문·선택지·매핑을 원문과 대조 검수
- 5개 결과가 모두 도달 가능한지 자동 테스트
- 결과 이미지 5종 문구·파일 대응 검수
- 느린 네트워크에서 로딩 확인
- 정적 호스팅 배포 후 여러 기기에서 QR 접속 확인

### 8단계 — 행사 직전 운영 점검

- 실제 배포 주소와 QR 코드 고정
- 버스 출발 전 현장 통신망에서 접속 확인
- 200명 규모의 정적 파일 동시 요청에 대한 간단한 부하 확인
- 장애 시 사용할 결과 링크/진행 안내 등 운영 백업안 준비

## 6. 아직 미확정인 결정사항

| 우선순위 | 결정사항 | 권장 기본안 |
|---|---|---|
| 필수 | 최고 점수 동점 처리 | 답변 시퀀스 기반 결정적 해시로 동점 후보 중 선택 |
| 확정 | 배포 호스팅과 최종 URL | GitHub Pages + `https://trace.greengroove.app` |
| 필수 | 결과 카드 5종의 최종 이미지 규격 | Figma에서 고해상도 PNG 5종 내보내기 |
| 필수 | 결과 화면의 저장/공유 UX | 공유 지원 시 공유, 그 외 다운로드/새 탭 안내 |
| 확인 | 질문·선택지·매핑 최종 동결 시점과 검수 담당자 | 구현 전 2인 대조 검수 |
| 확인 | 결과에서 점수 또는 점수 분포를 사용자에게 보여줄지 | 현재 목적상 숨김 |
| 확인 | 결과 화면에서 다시 하기 버튼을 제공할지 | 제공하되 확인 문구 없이 즉시 초기화 |
| 확인 | 1번 문항의 이전 버튼 동작 | 숨김 또는 시작 화면으로 돌아가기 중 Figma 기준 적용 |
| 선택 | 네트워크 불안 대비 PWA 오프라인 지원 | 첫 버전에서는 제외, 현장 통신 테스트 후 필요 시 추가 |
| 선택 | 익명 분석 도구 사용 | 개인정보·분포 미수집 원칙에 따라 제외 |

## 7. 완료 기준

- 20개 문항이 모두 A~E 5지선다로 정확히 표시된다.
- 선택 즉시 다음 문항으로 이동하고, 이전 문항 답변 수정이 정확히 반영된다.
- 답변 데이터가 기기나 서버에 남지 않고 새로고침 시 완전히 초기화된다.
- 모든 결과 문구와 시각 표현이 Figma 최종본과 일치한다.
- 동점 정책이 테스트로 고정되어 같은 답변에 일관된 결과가 나온다.
- 결과 이미지 저장 또는 공유가 주요 iOS/Android 브라우저에서 가능하다.
- 모바일 접근성, 터치 사용성, 느린 네트워크 조건을 통과한다.
- 서버나 데이터베이스 없이 정적 배포로 운영된다.
